spring:
  application:
    name: QueueSystem
  datasource:
    url: jdbc:postgresql://localhost:5432/Queue_System
    username: postgres
    password: Smit@123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: sonagarasmit566@gmail.com
    password: nyfu lkax aylk rvdg
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080
